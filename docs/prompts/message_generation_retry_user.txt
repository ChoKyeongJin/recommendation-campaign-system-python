이전 채널 메시지 생성 결과가 검증에 실패했다.
이번 응답은 실패 사유를 모두 수정해서 한국어 JSON object만 반환하라.

[Attempt]
${attempt_number}/${max_attempts}

[Failure Reason]
${failure_reason}

[Validation Issues]
${validation_issues}

[Previous Content]
${previous_content}

[Repair Rules]
- messages 배열은 정확히 3개만 포함한다.
- variant는 benefit_emphasis, urgency_emphasis, emotion_emphasis를 각각 1개씩만 사용한다.
- channel은 요청 채널과 반드시 일치한다. rcsSms 요청도 출력 channel은 rcs다.
- source_campaign_id는 Campaign Context에 존재하는 campaign_id만 사용한다.
- used_offer는 선택값이다. 메시지에서 Campaign Context의 offer를 직접 근거로 사용한 경우에만 원문 그대로 입력하고, 그렇지 않으면 null로 둔다.
- LMS는 text만 생성하고 통신사 기준 2,000Byte 이내로 작성한다. 한글/전각은 2Byte, ASCII는 1Byte다.
- RCS는 title 14자 이하, description 100자 이하, buttons 최대 1개, button name 7자 이하로 작성한다.
- RCS title에는 '(광고)'를 포함하고 description에는 '무료 수신거부'를 포함한다.
- 세 메시지는 동일한 본문이나 동일한 문장 구조를 반복하지 않는다.

[Original Prompt]
${original_prompt}